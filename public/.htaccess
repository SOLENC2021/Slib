<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Support React Router SPA fallback for clean client-side URLs
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.html [L]
</IfModule>
